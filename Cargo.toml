[package]
name = "spartan"
version = "0.0.0"
authors = ["Nazar Mokrynskyi <nazar@mokrynskyi.com>"]
edition = "2018"

[dependencies]
rug = "1.11.0"

[dependencies.rayon]
optional = true
version = "1.3.0"

[dev-dependencies]
rand = "0.8.1"

[features]
default = ["parallel"]
parallel = ["rayon"]
